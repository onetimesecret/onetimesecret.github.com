One-Time Secret Blog

## Initial setup

    $ sudo gem install jekyll rdiscount
    $ sudo gem uninstall liquid
    $ sudo gem install liquid --version 2.2.2
    $ sudo easy_install Pygments

## Previewing Changes

    $ jekyll --server

